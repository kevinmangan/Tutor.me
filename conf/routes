# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Index page
GET     /                           controllers.Application.index()

# Student index page
GET     /studentIndex                controllers.Application.studentIndex()

# Tutor index page
GET     /tutorIndex                  controllers.Application.tutorIndex()

# Student Register
POST    /studentRegister            controllers.Application.studentRegister()

# Home page
POST    /tutorRegister              controllers.Application.tutorRegister()



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
